# make.configuration.defn file for thorn ssht

# Define standard ssht utilities and install those that we could build/that are
# present in the system
STD_SSHT_UTILS = 
ALL_UTILS += $(STD_SSHT_UTILS)
